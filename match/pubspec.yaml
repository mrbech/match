name: match
description: Dart library with match annotation for generating custom match extensions and extension methods for dart builtin types
version: 0.4.0
homepage: https://github.com/mrbech/match

dependencies:
  collection: ^1.15.0

environment:
  sdk: '>=2.12.0 <3.0.0'

dev_dependencies:
  pedantic: ^1.9.0
  test: ^1.14.6
